# Fontes que dependem de navegador (Playwright + Chromium).
# Instale com: pip install playwright && playwright install chromium

# Bing Shopping — ligada por padrao. Alcanca ofertas de lojas grandes
# (sobretudo Amazon BR), que bloqueiam scraping direto. Custa ~8s por busca,
# porque sobe um Chromium. Desligue com 0 se quiser respostas mais rapidas.
BING_SHOPPING_ENABLED=1

# Google Shopping — desligada por padrao. O Google responde com pagina de
# verificacao anti-robo (CAPTCHA), entao hoje ela so reporta falha.
GOOGLE_SHOPPING_ENABLED=0
