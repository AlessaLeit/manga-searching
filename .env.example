# Fonte Google Shopping (Playwright).
# Desligada por padrao: exige Chromium instalado (`playwright install chromium`)
# e cada busca sobe um navegador headless, o que custa segundos por requisicao.
# As demais fontes (Estante Virtual, MangaDex) funcionam sem isto.
GOOGLE_SHOPPING_ENABLED=0
